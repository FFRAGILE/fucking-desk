name: "⚡ Advanced Unified Cloud — Windows & Linux"

on:
  workflow_dispatch:
    inputs:
      OS:
        description: "Select Operating System to Run"
        required: true
        default: "Windows"
        type: choice
        options:
          - "Windows"
          - "Linux (Ubuntu)"
      CODE:
        description: "Paste the headless command from https://remotedesktop.google.com/headless"
        required: true
      PIN:
        description: "CRD PIN (6+ digits). Default 123456"
        required: false
        default: "123456"
      COMPUTER_NAME:
        description: "Custom Computer Name in Google Remote Desktop"
        required: false
        default: "MyCloudPC"
      WARP:
        description: "Enable Cloudflare WARP (VPN/Proxy Bypass)"
        type: boolean
        required: false
        default: true

jobs:
  # ==============================
  # بخش اول: راه‌اندازی ویندوز ۱۰
  # ==============================
  windows-rdp:
    if: github.event.inputs.OS == 'Windows'
    runs-on: windows-latest
    steps:
      - name: "🔊 1. Pre-configure Audio Devices (Before GCRD Start)"
        shell: pwsh
        run: |
          Write-Host "Configuring and Starting Windows Audio Services..."
          Set-Service -Name "Audiosrv" -StartupType Automatic
          Set-Service -Name "AudioEndpointBuilder" -StartupType Automatic
          Start-Service -Name "Audiosrv", "AudioEndpointBuilder" -ErrorAction SilentlyContinue

          Write-Host "Installing VB-Audio Cable for Sound..."
          choco install vb-cable -y --no-progress
          
          Write-Host "Forcing Windows to reload audio endpoints..."
          Restart-Service -Name "Audiosrv", "AudioEndpointBuilder" -Force -ErrorAction SilentlyContinue

      - name: "🛡️ 2. Disable Firewall & Enable Terminal Server"
        shell: pwsh
        run: |
          Write-Host "Disabling Windows Firewall to allow WebRTC..."
          Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False

          Write-Host "Enabling Terminal Server (Remote Desktop) in Registry..."
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: "☁️ 3. Install & Start Cloudflare WARP (If selected)"
        shell: pwsh
        run: |
          if ("${{ github.event.inputs.WARP }}" -eq "true") {
              try {
                  Write-Host "Downloading Cloudflare WARP MSI directly from Cloudflare..."
                  $msiUrl = "https://www.cloudflarewarp.com/Cloudflare_WARP_Release-x64.msi"
                  $msiPath = "$env:TEMP\Cloudflare_WARP_Release-x64.msi"
                  Invoke-WebRequest -Uri $msiUrl -OutFile $msiPath
                  
                  Write-Host "Installing Cloudflare WARP silently..."
                  Start-Process msiexec.exe -ArgumentList "/i `"$msiPath`" /qn /norestart" -Wait
                  
                  Write-Host "Waiting 10 seconds for Cloudflare service initialization..."
                  Start-Sleep -Seconds 10
                  Get-Service -Name "Cloudflare WARP" | Start-Service -ErrorAction SilentlyContinue

                  $warpCli = "C:\Program Files\Cloudflare\Cloudflare WARP\warp-cli.exe"
                  if (Test-Path $warpCli) {
                      Write-Host "Registering and Connecting Cloudflare WARP..."
                      & $warpCli --accept-tos registration new
                      & $warpCli --accept-tos mode warp
                      & $warpCli --accept-tos connect
                      Write-Host "Cloudflare WARP connected successfully!"
                  } else {
                      Write-Host "WARP CLI path not found. You can still connect manually inside the RDP desktop."
                  }
              } catch {
                  Write-Host "⚠️ WARP connection failed to start automatically, but RDP boot will continue. You can open WARP manually inside desktop."
              }
          } else {
              Write-Host "Cloudflare WARP is disabled by user."
          }

      - name: "📥 4. Download Chrome Remote Desktop"
        shell: pwsh
        run: |
          Write-Host "Downloading Chrome Remote Desktop..."
          $msiUrl = "https://dl.google.com/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi"
          $msiPath = "$env:TEMP\crdhost.msi"
          Invoke-WebRequest -Uri $msiUrl -OutFile $msiPath
          
          Write-Host "Installing Chrome Remote Desktop..."
          Start-Process msiexec.exe -ArgumentList "/i `"$msiPath`" /qn /norestart" -Wait

      - name: "🌟 5. Connect & Start RDP (Windows)"
        shell: pwsh
        run: |
          $Code = @"
          ${{ github.event.inputs.CODE }}
          "@
          $Pin = "${{ github.event.inputs.PIN }}"
          $CustomName = "${{ github.event.inputs.COMPUTER_NAME }}"

          # Append PIN if not present
          if ($Code -notlike "*--pin=*") {
              $Code = $Code + " --pin=$Pin"
          }

          # Dynamically replace computer name parameters to use your custom name
          $Code = $Code -replace '--name=[^\s]+', "--name=`"$CustomName`""
          $Code = $Code -replace '--name-prefix=[^\s]+', "--name-prefix=`"$CustomName`""

          Write-Host "Executing GCRD command with custom name: $CustomName"
          Invoke-Expression $Code
          
          Write-Host "==============================================="
          Write-Host "Windows RDP configured under name: $CustomName"
          Write-Host "==============================================="

      - name: "🔁 6. Keep Alive Loop (Windows)"
        shell: pwsh
        run: |
          $timeout = New-TimeSpan -Hours 6
          $stopwatch = [System.Diagnostics.Stopwatch]::StartNew()
          while ($stopwatch.Elapsed -lt $timeout) {
              $remaining = $timeout - $stopwatch.Elapsed
              Write-Host "Running... Time elapsed: $($stopwatch.Elapsed.ToString('hh\:mm\:ss')) (Remaining: $($remaining.ToString('hh\:mm\:ss')))"
              Start-Sleep -Seconds 60
          }

  # ==============================
  # بخش دوم: راه‌اندازی اوبونتو لینوکس
  # ==============================
  linux-rdp:
    if: github.event.inputs.OS == 'Linux (Ubuntu)'
    runs-on: ubuntu-latest
    steps:
      - name: "📥 1. Install Desktop, PulseAudio & GCRD"
        run: |
          sudo apt-get update
          
          echo "Installing lightweight XFCE4 desktop environment..."
          sudo apt-get install -y xfce4 xfce4-goodies xvfb DBus-x11
          
          echo "Installing PulseAudio for sound support..."
          sudo apt-get install -y pulseaudio pavucontrol
          
          echo "Downloading & installing Chrome Remote Desktop..."
          wget https://dl.google.com/linux/direct/chrome-remote-desktop_current_amd64.deb
          sudo dpkg -i chrome-remote-desktop_current_amd64.deb || sudo apt-get install -f -y

      - name: "☁️ 2. Install & Connect Cloudflare WARP (If selected)"
        run: |
          if [ "${{ github.event.inputs.WARP }}" = "true" ]; then
              try {
                  echo "Adding Cloudflare repository..."
                  curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
                  echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
                  sudo apt-get update && sudo apt-get install -y cloudflare-warp
                  
                  echo "Registering and connecting Cloudflare WARP..."
                  warp-cli --accept-tos registration new
                  warp-cli --accept-tos connect
                  echo "Cloudflare WARP has been configured!"
              } catch {
                  echo "WARP auto-connect failed on Linux, but RDP setup will continue."
              }
          else
              echo "Cloudflare WARP is disabled by user."
          fi

      - name: "⚙️ 3. Configure Desktop Session & Pre-start Sound"
        run: |
          echo "Configuring XFCE session for Chrome Remote Desktop..."
          echo "exec /etc/X11/Xsession /usr/bin/xfce4-session" > ~/.chrome-remote-desktop-session
          
          echo "Adding user to chrome-remote-desktop group..."
          sudo usermod -a -G chrome-remote-desktop $USER
          
          echo "Initializing and starting PulseAudio Sound Server in background..."
          pulseaudio --kill || true
          pulseaudio --start --daemonize

      - name: "🌟 4. Connect & Start RDP (Linux)"
        run: |
          CODE_INPUT="${{ github.event.inputs.CODE }}"
          PIN_INPUT="${{ github.event.inputs.PIN }}"
          CUSTOM_NAME="${{ github.event.inputs.COMPUTER_NAME }}"
          
          # Clean up code format and ensure PIN is appended
          if [[ "$CODE_INPUT" != *"--pin="* ]]; then
            CODE_INPUT="$CODE_INPUT --pin=$PIN_INPUT"
          fi

          # Replace computer name/prefix in command string with your custom name
          CODE_INPUT=$(echo "$CODE_INPUT" | sed -E "s/--name=[^ ]+/--name=\"$CUSTOM_NAME\"/g")
          CODE_INPUT=$(echo "$CODE_INPUT" | sed -E "s/--name-prefix=[^ ]+/--name-prefix=\"$CUSTOM_NAME\"/g")
          
          echo "Executing Chrome Remote Desktop headless registration with name: $CUSTOM_NAME"
          eval "$CODE_INPUT"
          
          echo "==============================================="
          echo "Linux RDP is successfully configured!"
          echo "Go to https://remotedesktop.google.com/access"
          echo "And connect using your PIN: $PIN_INPUT"
          echo "==============================================="

      - name: "🔁 5. Keep Alive Loop (Linux)"
        run: |
          echo "Keeping runner alive for up to 6 hours..."
          timeout 6h bash -c '
            while true; do
              echo "Server is running..."
              sleep 60
            done
          '
